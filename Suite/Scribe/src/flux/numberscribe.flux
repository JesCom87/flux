// numberscribe.flux (toy)
scribe NumberScribe {
  fun assemble_from_ir(ir_lines, outpath) {
    // assume ir_lines is an array of strings already in textual OP format
    // write out each line to outpath
    // PSEUDO: write_lines(outpath, ir_lines)
  }
}
